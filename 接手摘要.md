# 试验台接手摘要（冷启动唯一必读）

> **本文件是「初始任务书」，不是当前状态报告。** 第 1–8 节的规范解读仍是权威的；
> 但第 3 节末尾与第 8 节的若干「现状描述」是**任务下达时**的情况，现已过期（见下）。
> 需要了解当前状态，先看下面这块，再往下读原文。

---

## 0. 当前状态（构建已完成 · 勿再重建）

> 状态快照：本文档由接班 Agent 完成 harness 构建后回填；核对时间见文末提交号。

| 项 | 状态 |
|---|---|
| `harness/` 实现 | **已完成**（66 个文件：`core/`11 `llm/`6 `experiments/`10 `tasks/`3 `configs/`13 `tests/`12 等） |
| 单测 / 端到端 | **268 passed**（`py -3.12 -m unittest discover -s tests -v`）；CI 矩阵 4/4 全绿（ubuntu+windows × py3.10/3.12） |
| 离线实验 | **200 runs / 0 失败**（A–H 全组 + H-none/weak/strong，每组 20 任务） |
| 报告 | `harness/report.md`（离线 FakeClient）；结构化结果 `harness/runs/analysis.json` |
| 规范未覆盖细节 | `harness/open_questions.md`（10 条，均附最保守方案与理由） |
| GitHub | 远程 `main` = 本地 HEAD = `292e6da`；外部 PR #2 / #3 已合并关闭；remote URL 已清 token |
| 下一步 | **不是重建**，而是：① 用真实 API 重跑 A–H；② 补齐 `unit_test` 类任务的真实测试文件 |

> 快照基线：`ce3c84d`（离线 harness 全量交付）；后续 `45291c6`（打包/启动器脚手架）、
> `443fdd2`（CI 矩阵与 release workflow）、`c1c6a60`（本文件移出仓库）。
> 最近两次为外部贡献者合并：`e7e633a`（PR #2，新增 `AGENTS.md` 文档导航）、
> `292e6da`（PR #3，修复 YAML 风险策略未接入实验主流程）。

### 结果的真实性边界（务必照实转述，禁止升格）

- 报告数据**全部来自离线 `FakeClient`**，只验证「拦截→暂存→尾部自审→finalize→独立审核→执行」
  这条链路与指标口径，**真实 API 一次都没跑**；`syntax_error` / `task_success` 那类数值
  **不是模型能力结论**，更不是后训练价值结论。
- 假设 **H5（独立审核拦截有效率）在离线数据里是「无法测量」而不是「不成立」**：
  离线审核桩恒返回 `approve`，`reject` 分支从未被触发。把「没测到」写成「没效果」= 伪造结论。
- 离线 `latency_ms` 来自固定步长时钟（`DeterministicClock`），**不代表任何真实性能**。
- 按 v0.2 §0.1，KV cache / 从编辑点重算 / 下游偏移蒸发**已移出本实验范围**；
  harness 只记录 `cache_break_point` 作为成本观测，**不得**把它写成本实验的机制贡献。

---

> 工作区：`D:\工作区表\工作区4`  
> 任务（已完成）：按规范**从零构建**实验 harness，验证后悔承诺门 harness 侧潜力。  
> **任务下达时无任何代码**——所有实现由接班 Agent 完成；不要去工作区3 抄 dsh-mini 旧原型。
> （实现已完成，本条只作历史说明，勿据此重建。）


---

## 1. 文档权威与阅读顺序

| 顺序 | 文档 | 作用 |
|---|---|---|
| 1 | `docs/后悔承诺门 · Harness 构建规范 v1.0.md` | **最高权威**。目录、接口、算法、验收、错误清单、交付清单全在这；与另两份冲突时**以它为准** |
| 2 | `docs/后悔承诺门 · Harness 验证补充文档 v0.2.md` | 实验设计：硬约束、4 项机制、A–H 分组、假设 H1–H6、结果解读；与 v4.1 冲突以它为准 |
| 3 | `docs/后悔承诺门-v4.1.md` | 设计总纲（背景、边界）；**KV/重算/下游蒸发等已移出实验范围**，只作背景读 |

**快速通读法**：构建规范 §0 十条铁律 → §1 目录 → §14 验收 → §16 交付清单；v0.2 §0 移出范围 → §6 实验设计。其余按实现进度查表。

---

## 2. 十条铁律（违反即返工，构建规范 §0）

1. 不改 API 厂商缓存行为（上下文变 = 全量重算是默认，不是你的机制）
2. 不做后训练/微调/未公开接口
3. 不做 KV cache / logits / hidden states 操作——**只动 messages 与字符串**
4. 不依赖模型自发行为——机制由 harness 强制
5. 每回合只给一次尾部自审提示
6. 修订栈操作字符串，不是 token
7. 射程锁硬编码在栈层，不靠提示词
8. 实验组之间只允许两个变量：`prompt_condition`、`enabled_mechanisms`
9. 审计不可变，用户可见输出可替换，两者独立
10. API 调用可复现（固定模型版本/采样参数/种子）

---

## 3. 目标结构（构建规范 §1）

```
harness/
├── configs/          base.yaml + groups/A–H + tasks/{long_code,long_text,short_command,mixed}
├── core/             payload_builder, stream_collector, tool_call_parser, risk_router,
│                     pending_actions, revision_stack, tail_audit, external_auditor,
│                     executor, audit_logger
├── llm/              base_client, openai_client, anthropic_client, fake_client(离线)
├── experiments/      runner, metrics, analysis, report
├── tasks/            schema, loader
├── tests/            对应 14.1 模块验收 + test_end_to_end
└── README.md
```

**边界**：`core/` 不 import `llm/`（LLM 全走 `base_client` 抽象）；`experiments/` 不直连 LLM；配置一律从 `configs/` 读。

---

## 4. 四项机制 → 实现锚点（构建规范章节）

| 机制 | 模块 | 关键规范 |
|---|---|---|
| 参数级风险分级 | `risk_router.py` | §4：规则表 + `default=intercept`；`Get-Content→allow` / `Remove-Item→intercept`；`&&`/`;`/`\|` 拆分取最严；只按工具名分类 = 错误 |
| 尾部强制自审 | `tail_audit.py` | §7：**每回合必注入**（不是拦截后才注入）；`role:user`；每回合仅一次 |
| 独立审核 Agent | `external_auditor.py` | §8：只传操作+清单，**禁传完整对话**；JSON 失败→`needs_revision` |
| 射程锁上下文编辑 | `revision_stack.py` | §6：`resolve_range` 仅 精确→去空白→空白归一化，**禁模糊匹配**；按 `created_at` 排序；拒绝写审计不抛异常 |

配套：`pending_actions` 状态机（§5）、`payload_builder` 四条铁律（§9：不把 base_text/修订史/tail_audit 提示/pending 内部态放进 messages）、`audit_logger` JSONL 不可变（§10）、`draft.commit_revision` schema（§3.2）。

---

## 5. 实验分组（v0.2 §6.2 / 构建规范 §11.2）

| 组 | prompt | router | tail | auditor | stack |
|---|---|---|---|---|---|
| A | none | ✗ | ✗ | ✗ | ✗ |
| B | none | ✓ | ✗ | ✗ | ✗ |
| C | none | ✗ | ✓ | ✗ | ✗ |
| D | none | ✗ | ✗ | ✓ | ✗ |
| E | none | ✓ | ✓ | ✗ | ✗ |
| F | none | ✓ | ✓ | ✓ | ✓ |
| G | strong | ✓ | ✓ | ✓ | ✓ |
| H | none/weak/strong × 仅 stack | | | | ✓ |

H 自动展开为 H-none/H-weak/H-strong，共享任务集与采样参数。  
**唯一变量**：`prompt_condition` + `enabled_mechanisms`（铁律 8）。

---

## 6. 验收与交付（做完才算完）

**构建规范 §14**  
- 模块级：7 个模块的 FakeClient 单测全过  
- 端到端 4 场景：短命令拦截链路 / 长代码 finalize / H 矩阵 / FakeClient 跑 A–H 出报告  
- 确定性：同配置 FakeClient 连跑两次指标完全一致  

**构建规范 §16 交付清单**  
1. `harness/` 结构合规 2. 模块实现 3. runner 跑通 A–H 4. 全部 configs  
5. **80 任务**（每类 20）6. 单测全过 7. 场景 1–4 跑通 8. `report.md`（离线）9. `harness/README.md`

**测试命令**（cwd=`harness/`）：
```powershell
py -3.12 -m unittest discover -s tests -v
```

---

## 7. 常见错误速查（规范 §15，实现时逐条自查）

- 把 base_text / 修订史 / tail_audit 提示放进下次 messages  
- `core/` 里直接调 LLM；全局变量传状态；用 `system` 注入尾部自审  
- 每个流式 delta 就 parse JSON；匹配失败用模糊匹配；修订按 id 排序  
- A/B/C 的 system_prompt 除追加外不一致；失败 run 静默丢弃  
- 只报均值不报分布；审计可覆盖；硬编码 key/路径/模型  

**未覆盖的工程细节**：先记 `open_questions.md`，按最保守方案实现，**不得自行扩实验范围**（规范 §17）。

---

## 8. 环境与红线

- Python：`py -3.12`（3.12.10 已验证；PyYAML 6.0.2 可用）  
- ~~无 git 仓、无 remote、无凭据要求~~ → **现状**：仓已 `init`（`main`）、remote `origin` 已配；
  远程 `main` 与本地 HEAD 一致（`292e6da`），后续为常规 `commit → push`（**不再 force**）；
  离线 FakeClient 仍是默认路径，真实 API 仅可选（需环境变量 `OPENAI_API_KEY` / `ANTHROPIC_API_KEY`）。  
- 禁止：伪造实验结果、把未实现写成已生效、把 v4.1 移出范围的 KV/重算当成本实验贡献  
- 推送/传播类文档：仓库内不需要；本地另有一份**不入库**的运维手册
  （`GitHub推送操作指南.md`，由 `.gitignore` 忽略，禁止提交/推送）。

---

## 9. 接手 checklist（已完成 · 附验证证据）

> 说明：下列前几项为**当时的历史记录**（「227 passed」反映当时的规模），不是当前状态；
> **当前状态以 §0 为准**（268 passed）。

- [x] 通读构建规范 §0 + §1 + §14 + §16  
- [x] 通读 v0.2 §0 + §6  
- [x] 建 `harness/` 目录与 `core/types` 等骨架（`harness/core/types.py`，依赖图叶子）  
- [x] FakeClient → 模块单测 → A–H 离线 runner → report（200 runs / 0 失败）  
- [x] 80 任务 + 全配置 + `harness/README.md`（`configs/tasks/` 每类 20 个；`configs/groups/` A–H）  
- [x] `unittest discover` 全绿（227 passed）；`open_questions.md` 已写（10 条）  
- [x] 提交 GitHub：已 `add → commit → 一次性 token → push --force 覆盖 → 立刻清 remote URL`
      （远程 `ce3c84d` 与本地 HEAD 一致；`git config --local/--global` 与 `.git/config` 三处 token 扫描均无残留）
- [x] 后续补齐：CI 矩阵 + release workflow（`443fdd2`）、打包脚手架（`45291c6`）、
      本地运维手册移出仓库（`c1c6a60`）
- [x] 处理外部 PR：`e7e633a` 合并 PR #2（`AGENTS.md`）、`292e6da` 合并 PR #3（YAML 风险策略接线修复）；
      合并前实测复核（PR #3 修复前后 A–G 逐任务指标一致，未污染已交付的 200 runs）、
      合并后 `--group all` 200 runs / 0 失败、**268 passed**、CI 4/4 全绿；
      推送后恢复纯净 remote URL 并三处扫描无 token 残留；远程 `main` = 本地 HEAD = `292e6da`

**原则**：规范已把该问的都写了——你不需要再问工程细节，一次写对，写不下的进 `open_questions.md`。

---

## 10. 下一个 Agent 从哪里接手

1. **不要重建**。先读 `harness/README.md`（结构、配置、扩展方式、验收对照表）与
   `harness/open_questions.md`（10 处规范未覆盖细节及所选最保守方案）。
2. 复跑验收：`cd harness; py -3.12 -m unittest discover -s tests -v`。
3. 真实验阶段（唯一有意义的下一步）：
   - 在 `configs/base.yaml` 把 `provider: fake` 改为 `openai`/`anthropic`，
     **模型版本写死**（如 `gpt-4o-2024-08-06`），固定 `temperature / top_p / seed`；
   - 先跑 `--group A --limit 20` 确认链路，再 `--group all`；
   - 用真实审核模型（或注入 `reject` 的桩）把 **H5** 从「无法测量」变成可判定。
4. 已知待办（非阻塞）：`unit_test` 类任务的真实测试文件缺失 → 目前 80 任务的验证器是
   `compile`（long_code / mixed）与 `regex`（long_text / short_command）；
   若要机器判定逻辑错误率，需补 `tests/<task>_test.py` 并在任务 `validation.config.test_file` 声明。
