# 协同进化 · 索引

> 建立时间：2026/09/26
> 用途：本目录是"模型 × harness 协同进化"方向的**全部本地资产**。
> **本目录不入库**（已在仓库根 `.gitignore` 中忽略 `/协同进化/`）。

---

## 0. 从哪读起

| 你的目的 | 先读 |
|---|---|
| **要开工做工程落地** | `工程实践项目总表.md` → `项目规格书 · 当前可做.md`（P1–P3） |
| 想知道**该做什么 / 先后顺序** | `工程实践项目总表.md` §1 决策门 + §3 依赖图 |
| 想知道**该做什么**（学术视角） | `思考轨迹分析/思考轨迹分析.md` §5–§6（四个空白 + 下一步） |
| 想知道**技术怎么做** | `KV缓存手术 · 工程设计与文献基础 v1.0.md` |
| 想知道**前人做了什么** | `Leyline与EVOKE精读笔记.md` |
| 想知道**为什么这条路值得走** | `KV局部编辑 · 研究方向备忘.md` |
| 想知道**和现有规范冲不冲突** | `规范缺口与阶段定位.md` |
| 想跑**实测** | `experiments/` |
| 想搭**微调环境** | `微调环境搭建.md` + `setup_微调环境.ps1` |

---

## 0.1 工程落地入口（新增）

| 文件 | 大小 | 内容 |
|---|---|---|
| `工程实践项目总表.md` | 8.4 KB | ★ **总纲**：G2 决策门、项目一览、依赖图、硬件约束、禁止事项、成功定义 |
| `项目规格书 · 当前可做.md` | 11.6 KB | ★ **P1–P3 详细任务**：提示词验证 / KV 正确性套件 / 微调闭环 |
| `工程项目清单.md` | 13.8 KB | **P4–P10**：G2 门 + 端点 / 对账 / GQA 核 / 稀疏 / 推理轨迹纠错 / 微调对照 |

**行动顺序**：`P1 ‖ P2 ‖ P3` → **P4（门）** → 按 P4 结果决定 P5–P9 → `P6 → P9`（最高价值路径）。

---

## 1. 文档

| 文件 | 大小 | 内容 |
|---|---|---|
| `README.md` | 12.6 KB | 资产总览（模型/源码/工具链/硬件现状） |
| `KV缓存手术 · 工程设计与文献基础 v1.0.md` | **59.3 KB** | ★ **主设计文档**：统一原语、8 条不变式、协议、成本模型、实验臂 K0–K6、实施路线、附录 A/B/C |
| `Leyline与EVOKE精读笔记.md` | 18.3 KB | ★ 两篇对标论文的精读、三层裁定、交叉对比 |
| `KV局部编辑 · 研究方向备忘.md` | 13.1 KB | 开题材料：问题定义、缓存一致性类比、R1–R7 路径 |
| `思考轨迹分析/思考轨迹分析.md` | 21.8 KB | ★ **直觉 vs 论文逐条对照**、思考模式识别、G1–G4 空白 |
| `规范缺口与阶段定位.md` | 7.8 KB | 与构建规范铁律 2/3 的冲突及处置建议 |
| `微调环境搭建.md` | 8.8 KB | 环境、依赖、4B 对照模型获取、显存现实 |
| `版本与来源清单.md` | 5.8 KB | commit hash / 文件 hash / 来源 URL |

---

## 2. 原始材料（`docs_收集/`）

| 文件 | 大小 | 说明 |
|---|---|---|
| `Leyline_2606.01065v1.pdf` | 745.5 KB | arXiv:2606.01065v1 原件 |
| `Leyline_全文.txt` | 143.7 KB | 抽取正文（3,169 行） |
| `EVOKE_zenodo20467232_paper-v2.pdf` | 490.5 KB | Zenodo 10.5281/zenodo.20467232 原件 |
| `EVOKE_全文.txt` | 104.5 KB | 抽取正文（1,576 行） |
| `hosts.bak.*` | 1.4 KB | **加 hosts 前的原始 hosts 备份**（若要还原 Zenodo 解析，用这个） |

---

## 3. 会话原料（`思考轨迹分析/`）

| 文件 | 大小 | 说明 |
|---|---|---|
| `用户消息全文.md` | 21.3 KB | **31 条用户消息逐字**（未改写） |
| `对话完整重建.md` | 178.3 KB | 完整时间线（295 助手 + 31 用户） |
| `助手推理链.md` | 286.2 KB | 1,605 段推理链 |
| `session_原始.jsonl` | 7.8 MB | 解压后原始 10,076 条记录 |

⚠️ **`session.jsonl.zstd` 是多帧压缩**：单次 `decompress()` 只出 191 字节（首帧），
必须用 `zstandard` 的 `stream_reader`。详见 `experiments/extract_session.py`。

---

## 4. 可复现脚本（`experiments/`）

| 脚本 | 作用 |
|---|---|
| `verify_kv_replace_context.py` | 实测"带旧 KV 算"vs"先删再算"（附录 A 的 70 万倍差距） |
| `verify_delete_insert_damage.py` | 实测 delete/insert 的 KV 破坏 + **噪声底**（附录 B） |
| `extract_session.py` | 解压多帧 zstd 会话日志 |
| `rebuild_conversation.py` | 重建对话与用户消息 |
| `fetch_leyline.py` / `fetch_evoke.py` / `fetch_papers.py` | 论文抓取 |

**运行环境**：`C:\Users\Administrator\qwen-gguf-build\venv\Scripts\python.exe`
（已装 `torch 2.14.0+cpu`、`transformers`、`pypdf`、`zstandard`）

⚠️ 含中文输出的脚本需 `$env:PYTHONIOENCODING = "utf-8"`，否则 GBK 控制台会报
`UnicodeEncodeError`。

---

## 5. 大型资产（不在本文档清单内，另有说明）

| 路径 | 内容 | 大小 |
|---|---|---|
| `models/Qwen3-0.6B/` | 原始 bf16 权重（**已三重核验为官方原版**） | 1.45 GB |
| `sources/ik_llama.cpp/` | ★ KV 改造目标（HEAD `85a3f2c`） | 143.6 MB |
| `sources/llama.cpp/` | 上游对照（HEAD `81bc6b8`） | 203.2 MB |
| `finetune/Qwen3/` | 官方微调文档与示例 | 35.9 MB |
| `finetune/ms-swift/` | 阿里官方微调框架 | 40.9 MB |
| `.venv-finetune/` | 微调环境（**尚未创建**，见 `setup_微调环境.ps1`） | — |

---

## 6. 下一步（三件事，按性价比）

1. **G2 测量**：在 Qwen3-0.6B 上测"删除/替换对后缀的影响半径"。
   **纯测量，成本最低，结果决定后面值不值得做。**
2. **G1 读附录**：读 Leyline 的 **App. H**（GQA/MHA 验证）与 **App. K**（CacheBlend/EPIC 映射），
   确认 GQA + 稀疏的空白是否仍在。
3. **G4 最高价值**：推理轨迹（thinking trace）纠错实验。
   你的 `ik_llama.cpp` 是**纯注意力栈**，不会撞上 EVOKE 的 Mamba 不可切片障碍。

**当前阶段（纯提示词潜力验证）不受以上影响，继续按原计划走。**

---

## 7. 环境注意事项

| 项 | 说明 |
|---|---|
| **hosts 修改** | 已加 `188.184.103.118 zenodo.org`（本机 DNS 把 zenodo.org 投毒为 `0.0.0.0`）。**用户要求保留**，便于后续查论文。原始 hosts 备份在 `docs_收集/hosts.bak.*` |
| `cmake` | **不在 PATH**，编译 ik_llama.cpp 前需补（E 盘备份里有 `cmake.zip`） |
| `nvcc` | ✅ 已有 `D:\Program Files\bin\nvcc.exe` |
| GPU | GTX 1050 Ti，**4 GB**，compute 6.1 —— 微调的硬瓶颈 |
| huggingface.co | ❌ 不可达；用 `HF_ENDPOINT=https://hf-mirror.com` |
