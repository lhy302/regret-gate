# secrets — 严禁入库
token*
*.token
.env
.env.*
# 模板文件必须入库（里面不含任何真实密钥，只是环境变量写法示例）
!.env.example
!**/.env.example
*.pat

# python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# 本地运行产物
*.log
*.jsonl
!configs/**/*.yaml
runs/
metrics.jsonl
report.md
# 交付清单要求 harness/report.md 入库（离线实验报告），单独放行
!harness/report.md

# 打包产物与运行输出
harness/build/
harness/dist/
harness/release/
harness/smoke/
harness/output/
output/
launcher_config.json
selftest.json

# 本地运维备忘 —— 严禁入库
#
# GitHub推送操作指南.md 是纯本地 runbook（含一次性令牌的操作流程）。
# 它历史上被提交过，后来专门做了一次历史改写 + gc 才彻底清干净，不要再让它回来。
# 若发现它在待提交列表里：git restore --staged "GitHub推送操作指南.md"
#
# 注意顺序：git 取**最后一条**匹配规则，因此宽规则在前、精确规则在后，
# 这样 `git check-ignore -v` 的输出会直接命中下面那条，便于自查时一眼看懂。
*推送*指南*.md
*推送*.md
/GitHub推送操作指南.md

# 编辑器 / 系统
.vscode/
.idea/
Thumbs.db
Desktop.ini

# 协同进化阶段 —— 文档入库，大型资产不入库
#
# 背景：`协同进化/` 里既有 GB 级的第三方资产（模型权重 / 推理框架源码 / 微调工具链），
# 也有可复用的工程文档与实测脚本。前者必须排除（仓库会爆炸，且可从上游重新获取），
# 后者应当入库（别人要照着工程稿做实践）。
#
# ⚠️ 规则顺序至关重要：git 取**最后一条匹配**的规则。若先写忽略目录、再写放行，
#    会因后续"整体忽略"规则再次命中而失效。因此本段必须先放行、后排除。
#
# 自查（两条都要过）：
#   git check-ignore -v 协同进化/索引.md        → 应【无输出】（会被跟踪）
#   git check-ignore -v 协同进化/models         → 应【有输出】（被忽略）

# --- 1. 先放行目录本身（否则 git 不会递归进被忽略的目录）---
!/协同进化/
!/协同进化/**/

# --- 2. 放行要入库的文档与脚本 ---
!/协同进化/*.md
!/协同进化/*.ps1
!/协同进化/experiments/*.py
!/协同进化/思考轨迹分析/*.md
!/协同进化/docs_收集/*.md
!/协同进化/docs_收集/*.txt

# --- 3. 最后排除大型资产与会话原始转储（放在末尾才会生效）---
/协同进化/models/
/协同进化/sources/
/协同进化/finetune/
/协同进化/.venv-finetune/
/协同进化/思考轨迹分析/session_原始.jsonl
/协同进化/思考轨迹分析/助手推理链.md
/协同进化/docs_收集/*.pdf
/协同进化/docs_收集/hosts.bak.*
__pycache__/


